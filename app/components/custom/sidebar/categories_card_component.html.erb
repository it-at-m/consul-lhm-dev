<% if @categories.present? %>
  <%= render Shared::SidebarCardComponent.new(title: t("shared.tags_cloud.categories"), icon_name: "info-circle") do |c| %>
    <%= render "shared/categories", taggable: "projekt", categories: @categories %>
  <% end %>
<% end %>
