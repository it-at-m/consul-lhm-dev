<% if @categories.present? %>
  <%= render Shared::SidebarCardComponent.new(title: t("shared.tags_cloud.categories"), icon_name: "tag", collapsed_on_mobile: true) do |c| %>
    <%= render "shared/categories", taggable: (@options[:taggable] || "projekt"), categories: @categories %>
  <% end %>
<% end %>
