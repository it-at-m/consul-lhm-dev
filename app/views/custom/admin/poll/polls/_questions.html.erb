<h3 class="inline-block"><%= t("admin.polls.show.questions_title") %></h3>

<%= link_to t("admin.questions.index.create"), polymorphic_path([@namespace, :question], action: :new, poll_id: @poll.id),
            class: "button float-right" %>

<%= link_to t("custom.admin.polls.poll.send_notifications"), polymorphic_path([@namespace, @poll], action: :send_notifications),
            class: "button float-right", method: :put, data: { confirm: t("custom.admin.polls.poll.send_notifications_confirm") } %>

<% if @poll.questions.empty? %>
  <div class="callout primary margin-top">
    <%= t("admin.polls.show.no_questions") %>
  </div>
<% else %>
  <table class="margin selectable">
    <thead>
      <tr>
        <th><%= t("admin.polls.show.table_title") %></th>
        <th><%= t("admin.actions.actions") %></th>
      </tr>
    </thead>
    <tbody class="sortable" data-js-url="<%= polymorphic_path([@namespace, @poll, :questions], action: :order_questions) %>" >
      <% @poll.questions.each do |question| %>
        <tr id="<%= dom_id(question) %>" data-answer-id="<%= question.id %>">
          <td>
            <strong><%= question.title %></strong>
            <% if question.allows_multiple_answers? %>
              <small>
                (<%= t("admin.questions.index.multiple") %>)
              </small>
            <% end %>
            <% if question.proposal.present? %>
              <small>
                <%= link_to t("admin.polls.show.see_proposal"),
                            proposal_path(question.proposal),
                            target: "_blank" %>
              </small>
            <% end %>
          </td>
          <td>
            <%= render Admin::TableActionsComponent.new(question) do |actions| %>
              <%= actions.link_to t("admin.polls.show.edit_answers"), polymorphic_path([@namespace, question]),
                                                                      class: "answers-link" %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
