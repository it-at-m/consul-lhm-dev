<% form_path = @projekt_label.persisted? ? admin_projekt_phase_projekt_label_path(@projekt_phase, @projekt_label) : admin_projekt_phase_projekt_labels_path(@projekt_phase, @projekt_label) %>

<%= translatable_form_for @projekt_label, url: form_path do |f| %>
  <%= render "shared/errors", resource: @projekt_label %>
  <%= render "shared/globalize_locales", resource: @projekt_label %>

  <%= f.translatable_fields do |translations_form| %>
    <div class="small-12 column">
      <%= translations_form.text_field :name %>
    </div>
  <% end %>

  <div class="clear">
    <div class="small-12 medium-6 large-4 column">
      <%= f.text_field :color, placeholder: t("custom.admin.projekt_phases.projekt_labels.form.color_placeholder") %>
    </div>
  </div>

  <div class="clear">
    <div class="small-12 column">
      <%= f.label :icon %>
      <div class='admin-deficiency-report-category-icons-selector'>
        <%= f.collection_radio_buttons :icon, Iconable::AVAILABLE_ICONS_SHORT, :first, :first do |rb| %>
          <%= rb.label { rb.radio_button + "<i class=\"fas fa-#{rb.object.first}\"></i>".html_safe } %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="small-12 column">
    <%= f.submit class: "button success" %>
  </div>
<% end %>
