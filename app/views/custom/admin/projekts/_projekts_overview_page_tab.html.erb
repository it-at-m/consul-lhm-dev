<h2><%= t("custom.admin.projekts.overview_page_tab.title") %></h2>
<table>
  <thead>
    <tr>
      <th><%= t("admin.settings.setting") %></th>
      <th><%= t("admin.settings.index.features.enabled") %></th>
    </tr>
  </thead>
  <tbody>
    <% @projekts_overview_page_navigation_settings.each do |feature| %>
      <tr>
        <td class="small-9">
          <strong id="<%= dom_id(feature, :title) %>"><%= t("custom.settings.#{feature.key}") %></strong>
          <br>
          <span class="small">
            <%= t("custom.settings.#{feature.key}_description", default: t("admin.settings.no_description")) %>
          </span>
        </td>

        <td>
          <%= render Admin::Settings::FeaturedSettingsFormComponent.new(feature, tab: "#tab-projekts-overview-page") %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2><%= t("custom.admin.projekts.overview_page_tab.title_footer") %></h2>
<table>
  <thead>
    <tr>
      <th><%= t("admin.settings.setting") %></th>
      <th><%= t("admin.settings.index.features.enabled") %></th>
    </tr>
  </thead>
  <tbody>
    <% @projekts_overview_page_footer_settings.each do |feature| %>
      <tr>
        <td class="small-9">
          <strong id="<%= dom_id(feature, :title) %>"><%= t("custom.settings.#{feature.key}") %></strong>
          <br>
          <span class="small">
            <%= t("custom.settings.#{feature.key}_description", default: t("admin.settings.no_description")) %>
          </span>
        </td>

        <td>
          <%= render Admin::Settings::FeaturedSettingsFormComponent.new(feature, tab: "#tab-projekts-overview-page") %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
