$(".js-projekts-list .js-resources-list--body").html("<%= j render(Projekts::ListComponent.new(projekts: @projekts, map_coordinates: @map_coordinates, only_content: true)) %>");

App.Map.initialize();
