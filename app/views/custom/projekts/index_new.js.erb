$(".js-projekts-list .js-resources-list--body").html("<%= j render(Projekts::ListComponent.new(projekts: @projekts, filters: @current_active_orders, current_filter: @current_order, map_coordinates: @map_coordinates, only_content: true)) %>");

App.Map.initialize();
