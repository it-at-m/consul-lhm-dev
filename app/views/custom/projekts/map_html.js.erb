<%= render Shared::MapComponent.new(
  map_location: @projekt.map_location,
  parent_class: "proposal",
  editable: true,
  map_layers: @projekt.map_layers_for_render.to_json,
  admin_shape: @projekt&.map_location&.shape
) %>
