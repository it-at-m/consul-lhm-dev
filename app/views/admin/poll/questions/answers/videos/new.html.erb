<%= back_link_to polymorphic_path([@namespace, @answer, :videos]) %>

<h2>
  <%= t("admin.answers.videos.new.title") %>
</h2>

<div class="poll-question-answer-video-form">
  <%= render "admin/poll/questions/answers/videos/form", form_url: polymorphic_path([@namespace, @answer, :videos]) %>
</div>
